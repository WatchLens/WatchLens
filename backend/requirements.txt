# FastAPI
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Settings
pydantic-settings==2.1.0

# Utils
python-dateutil==2.8.2

# ML Recommendations
recbole>=1.2.0
torch>=2.0.0
# scikit-learn is also pulled in by RecBole transitively, but the
# `similarity` recommender (TF-IDF cosine) imports it directly so pin
# it explicitly.
scikit-learn>=1.5.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.4
httpx==0.26.0
